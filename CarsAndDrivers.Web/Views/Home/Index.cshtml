@{
    ViewBag.Title = "Home Page";
}

<section class="row">
    <!-- latest 4 car posts -->
    @Html.Action("NewCarProfiles")
    <!-- Aside Ad Top left -->

    <aside class="col-md-4 col-sm-12">
        @Html.Action("AsideAd")
    </aside>
</section>
<section class="row">
    <!-- latest 4 registered drivers -->
    @Html.Action("NewDrivers")
    <!-- Aside Comments-->
    <aside class="col-md-4 col-sm-12">
        @Html.Action("LatestComments")
    </aside>
</section>
<section class="row">
    <div class="col-md-8 col-sm-12">
        <div class="panel panel-default text-center">
            @Html.Action("OnlineUsers")
            <!-- Top Car Posts of the Week car posts 1 -->
            @Html.Action("TopCarProfiles")
        </div>
        <!-- Aside Ad Bottom -->
        @Html.Action("BottomAd")
    </div>
    <!-- Aside Social Networks-->
    <aside class="col-md-4 col-sm-12 text-center">
        @Html.Action("SocialNetworks")
        <!--Best Driver by Admin-->
        <div class="panel panel-default">
            @Html.Action("BestDriver")
        </div>
    </aside>
</section>